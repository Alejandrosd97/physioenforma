---
import {tratamientosInfo} from'../../public/tratamientos.js'
import Tratamiento from "./Tratamiento.astro"
---



<style>

@keyframes fadeIn {
        from {
            transform : translateY(50px)
        }
        to {
            opacity : 1;
            transform: translate(0);
        }
    }

    .animationVisible {
        animation : 1s linear fadeIn;
        animation-fill-mode: forwards;
    }

</style>

<section id="servicios" class="mt-32">
    <div class="w-5/6 mx-auto text-center">
        <div class="mb-20">
            <h2 class="text-5xl text-slate-800">Descubre como podemos ayudarte</h2>
            <p class="text-gray-700 text-lg mt-4 md:w-4/6 m-auto w-5/6">En Physioenforma, nos dedicamos a brindarte una atención integral para recuperar y mejorar tu bienestar físico. Descubre cómo podemos ayudarte a sentirte en tu mejor forma física</p>
        </div>
        <div id="observableElementServicios" class="grid grid-cols-12 w-full mx-auto gap-x-5 gap-y-12">
            {
                tratamientosInfo.tratamientosFisioterapia.map((t, index)=>{
                    return  <Tratamiento servicio={t}/>
                })
            }
        </div>
    </div>
    
<script>
    const observableElement = document.getElementById('observableElementServicios')
    const servicios = document.getElementsByClassName('elementoServicio')
   
    let options = {
    root: document.querySelector("#scrollArea"),
    rootMargin: "0px",
    threshold: 0.2,
};

let observer = new IntersectionObserver((entries)=>{
    const entry =  entries[0]
    if (entry.isIntersecting){
        for (let servicio of servicios) {
            servicio.classList.add('animationVisible')
            }
        }
    }, options);

observer.observe(observableElement)

</script>
</section>