---
const { servicio : {id, titulo, descripcionCorta, img} } = Astro.props;

---


<style>

    @keyframes fadeIn {
            from {
                transform :translateY(50px)
            }
            to {
                opacity : 1;
                background-color: red;
                transform :translateY(0)
            }
        }
    
        .animationVisible {
            animation : 1s linear fadeIn;
            animation-fill-mode: forwards;
        }

        .elementoServicio{
            border-top-left-radius: 24px;
            border-top-right-radius: 24px;
        }
    
    </style>

<article class="elementoServicio opacity-0 border border-slate-100 shadow-lg col-span-12 sm:col-span-6 md:col-span-4">
        <img class="rounded-2xl h-[240px] w-full object-cover" src={img} alt="">
        <div class="px-4 py-6 mt-2">
            <h4 class="text-2xl mb-2 text-slate-900">{titulo}</h4>
            <p class="mb-8 text-gray-700">{descripcionCorta}</p>
            <a class="bg-slate-800 text-white py-4 px-8 rounded-full inline-block hover:bg-slate-700 duration-500"  href=`/tratamiento/${id}`>Mas información</a>
        </div>
    </article>